{
  "version": 1,
  "hooks": {
    "stop": [
      { "command": "${CURSOR_PLUGIN_ROOT}/capture.sh" }
    ],
    "sessionEnd": [
      { "command": "${CURSOR_PLUGIN_ROOT}/capture.sh" }
    ],
    "afterAgentResponse": [
      { "command": "${CURSOR_PLUGIN_ROOT}/capture.sh" }
    ]
  }
}
